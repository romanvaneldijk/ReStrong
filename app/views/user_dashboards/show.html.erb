<div class="cards">
  <div class="menu d-flex align-items-center justify-content-center">
  <strong><h1>Checkout</h1></strong>
  </div>
  <%= render partial: "collapse" %>
  <div class="d-flex justify-content-center">
      <%= link_to "new order" , menu_path ,{:style=>'color:white; text-decoration:none;',:class=>'button text-nowrap'} %>
      <button type="button" class="button" data-toggle="modal" data-target="#FINAL">PAY</button>
  </div>
</div>
<%= render partial: "modal" %>
